# OpenID Mini Frontend

Separate Vite + React frontend for the OpenID Mini auth server.

## Run

```bash
npm install
npm run dev
```

Default frontend URL:

```txt
http://localhost:5173
```

Make sure the backend `.env` contains:

```txt
FRONTEND_URL=http://localhost:5173
```
